import { Authenticator } from "@aws-amplify/ui-react";

const Group = () => {
    return (
        <Authenticator>
            {
                
            }
        </Authenticator>
    );
};

export default Group;